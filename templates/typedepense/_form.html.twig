{{ form_start(form) }}
<div class="row">
    <div class="col-md-6">{{ form_row(form.account_number) }}</div>
    <div class="col-md-6">{{ form_row(form.libelle) }}</div>
</div>
{{ form_rest(form) }}
<button class="btn btn-primary">{{ button | default('Enregistrer') }}</button>
{{ form_end(form) }}